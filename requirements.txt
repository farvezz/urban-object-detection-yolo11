# Wheel torch versi CPU-only. Streamlit Community Cloud tidak punya GPU, dan tanpa
# baris --extra-index-url di bawah pip akan menarik paket CUDA (~2 GB) yang membuat
# build free-tier melambat drastis atau gagal karena limit penyimpanan.
# JANGAN hapus baris ini atau akhiran +cpu pada pin torch/torchvision.
--extra-index-url https://download.pytorch.org/whl/cpu

torch==2.8.0+cpu
torchvision==0.23.0+cpu

ultralytics==8.4.67
streamlit==1.58.0
pillow==10.3.0
numpy==2.2.6
matplotlib==3.10.8
opencv-python-headless==4.13.0.92
