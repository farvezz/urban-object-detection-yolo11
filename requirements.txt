# Wheel torch versi CPU-only. Streamlit Community Cloud tidak punya GPU, dan tanpa
# baris --extra-index-url di bawah pip akan menarik paket CUDA (~2 GB) yang membuat
# build free-tier melambat drastis atau gagal karena limit penyimpanan.
# JANGAN hapus baris ini atau akhiran +cpu pada pin torch/torchvision.
--extra-index-url https://download.pytorch.org/whl/cpu

torch==2.8.0+cpu
torchvision==0.23.0+cpu

ultralytics==8.4.67
streamlit==1.58.0
pillow==10.3.0
numpy==2.2.6
matplotlib==3.10.8

# ultralytics mensyaratkan `opencv-python` (bukan varian headless), jadi paket itu
# selalu ikut terpasang dan varian headless tidak pernah bisa menggantikannya --
# keduanya menulis ke direktori `cv2/` yang sama. Karena itu:
#   - versinya di-pin di sini supaya resolver tidak melompat ke OpenCV 5.x yang
#     dirilis setelah ultralytics 8.4.67 dan belum diuji dengannya;
#   - `libgl1` + `libglib2.0-0` disediakan lewat `packages.txt`, karena build
#     opencv non-headless butuh libGL yang tidak ada di kontainer Streamlit Cloud.
opencv-python==4.13.0.92
